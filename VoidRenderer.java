/**
 * Void Renderer implements Renderer
 */
public class VoidRenderer implements Renderer  {

    /**
     * An empty Renderer implementation that doesn't actually display anything on the screen,
     * but is still a form of rendering
     * @param board
     */
    @Override
    public void renderBoard(Board board) {}
}
